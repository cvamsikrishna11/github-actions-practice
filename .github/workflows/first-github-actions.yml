name: Run Shell Commands
on: [push]

jobs:
    run-shell-command:
        runs-on: ubuntu-latest
        steps:
            - name: Echo a string
              run: echo "Hello there! This is the first GitHub Actions Workflow File! Long way to go :) "